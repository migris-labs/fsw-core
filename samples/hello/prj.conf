# Minimum Zephyr application configuration for the fsw-core hello-world.
# Just enough to print to the UART console on boot; no logging backend,
# no networking, no shell. Slice fsw-2.

CONFIG_PRINTK=y
CONFIG_BOOT_BANNER=y
CONFIG_THREAD_NAME=y
CONFIG_LOG=n
